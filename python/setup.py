from distutils.core import setup
from setuptools import find_packages

setup(name='OpenAq',
      version='1.0',
      description='Data pipeline for air data in Belgium',
      author='Romain Henneton',
      url='https://github.com/rhenneton/openaq',
      packages=find_packages(),
      )
